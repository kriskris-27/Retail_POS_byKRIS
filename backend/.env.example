# Server port (defaults to 5000 if not set)
PORT=5000

# Mongo connection string
MONGO_URI=mongodb+srv://username:password@cluster0.mongodb.net/retail

# JWT secret for signing auth tokens
JWT_SECRET=change_me

# Environment flags
NODE_ENV=development

# Domain used for secure cookies in production (e.g., your-app.com)
COOKIE_DOMAIN=your-domain.com
